@extends('layouts.app')

@section('content')
    <div class="row"><br><br>
        <div class="col-lg-12 margin-tb">
            <div class="pull-left">
                <h3>an application that can calculate the salary of employees</h3>
                <h4>Edit Employee</h4><br><br>
            </div>
            <div class="pull-right">
                <a class="btn btn-primary" href="{{ route('projects.index') }}" title="Go back"> <i
                        class="fas fa-backward "></i> </a>
            </div>
        </div>
    </div>

    @if ($errors->any())
        <div class="alert alert-danger">
            <strong>Whoops!</strong> There were some problems with your input.<br><br>
            <ul>
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif

    <form action="{{ route('projects.update', $project->id) }}" method="POST">
        @csrf
        @method('PUT')

        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Name:</strong>
                    <input type="text" name="name" value="{{ $project->name }}"
                           class="form-control" placeholder="Name">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Age:</strong>
                    <input type="text" name="age" value="{{ $project->age }}"
                           class="form-control" placeholder="Age">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Kids:</strong>
                    <input type="text" name="kids" value="{{ $project->kids }}"
                           class="form-control" placeholder="Kids">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Rent Car:</strong>
                    <input type="text" name="rentcar" value="{{ $project->rentcar }}"
                           class="form-control" placeholder="Rent Car">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Salary Rate:</strong>
                    <input type="text" name="salaryrate" class="form-control" placeholder="{{ $project->salaryrate }}"
                        value="{{ $project->salaryrate }}">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                    <strong>Tax Plan %:</strong>
                    <input type="number" name="tax" class="form-control" placeholder="{{ $project->tax }}"
                        value="{{ $project->tax }}">
                </div>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 text-center">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </div>

    </form>
@endsection
